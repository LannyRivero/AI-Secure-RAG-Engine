spring:
  application:
    name: ai-secure-rag-engine
  profiles:
    active: dev
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.0
      connection-timeout: 5s
      read-timeout: 10s

app:
  llm:
    provider: openai   # stub | openai
